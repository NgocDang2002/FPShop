<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link r ="stylesheet" href="/css/font-awesome6.1.1.min.css" />
    <link rel="stylesheet" href="/css/partials/header.css">

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css"
      integrity="sha512-KfkfwYDsLkIlwQp6LFnl8zNdLGxu9YAA1QvwINks4PhcElQSvqcyVLLD9aMhXd13uQjoXtEKNosOWaZqXgel0g=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <!-- bootstrap 2 -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA==" crossorigin="anonymous" referrerpolicy="no-referrer" />

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://getbootstrap.com/docs/5.3/assets/css/docs.css" rel="stylesheet">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm" crossorigin="anonymous"></script>
    <!-- bootstrap -->
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/base.css" />
    <link
      rel="icon"
      href="/images/favicon_io/favicon.ico"
      type="image/x-icon"
    />
    <script
      src="https://kit.fontawesome.com/92599c8cb3.js"
      crossorigin="anonymous"
    ></script>

    <style>
      /* Hide scrollbar for Chrome, Safari and Opera */
      ::-webkit-scrollbar {
        display: none !important;
      }

      /* Hide scrollbar for IE, Edge and Firefox */
      body {
        -ms-overflow-style: none; /* IE and Edge */
        scrollbar-width: none; /* Firefox */
      }
      .header-user-fullname:hover {
        cursor: pointer;
        opacity: 0.8;
      }
      .modal table {
        width: 100%;
      }
      .modal table input[type='text'],
      .modal table input[type='password'] {
        width: 100%;
        padding: 4px 8px;
      }

      .modal table td {
        padding: 10px;
      }
      .reset-pw-btn {
        width: 100%;
        padding: 4px;
        background-color: var(--topicColor);
        color: #fff;
        border-color: none;
        text-align: center;
        border-radius: 2px;
      }
      .reset-pw-btn:hover {
        cursor: pointer;
        opacity: 0.9;
      }
      .username-input:hover {
        cursor: not-allowed;
      }
    </style>
    <title>MỸ PHẨM FPSHOP</title>
  </head>
  <body>
    <header class="header">
      <div class="header-nav" id="Myheader">
        <div class="header_nav head-bn tb">
          <div class="header_nav_share">
            <div class="img">
              <a href="/">
                <img style="margin-left: 22px; font-size: 120px;" src="/images/test2.png" alt="">
              </a>
            </div>
            <div class="menu-header">
              <ul class="menu_ul" id="menu_test">
                <li><a href="/">TRANG CHỦ</a></li>
                <li><a href="/products">SẢN PHẨM</a></li>
                <li>
                  <a href="/introduce">GIỚI THIỆU</a>
                </li>
                <li>
                  <li><a href="/recruitment">TUYỂN DỤNG</a></li>
                </li>
                <li><a href="/news">TIN TỨC</a></li>
                <li><a href="/stores">LIÊN HỆ</a></li>
              </ul>
            </div>

            <!-- đối với admin -->

            <%if(userFName) {%> <%if(userRole==='admin') {%>
            <div class="header-admin-list">
              <div class="header-admin-list-text">QUẢN LÝ</div>

              <div class="header-admin-list-child">
                <a href="/users/admin">
                  <div class="header-admin-list-child-link">
                    DANH SÁCH NGƯỜI DÙNG
                  </div>
                </a>
                <hr style="width: 100%" />
                <a href="/orders/admin">
                  <div class="header-admin-list-child-link">
                    DANH SÁCH ĐƠN HÀNG
                  </div>
                </a>
                <hr style="width: 100%" />
                <a href="/products/admin">
                  <div class="header-admin-list-child-link">
                    DANH SÁCH SẢN PHẨM
                  </div>
                </a>
                <hr style="width: 100%" />
                <a href="/statistical/admin">
                  <div class="header-admin-list-child-link">
                    THỐNG KÊ
                  </div>
                </a>
              </div>
            </div>

            <!-- đối với khách hàng -->

            <%}%> <%if(userRole==='user') {%>
            <a class="header-order" href="/orders/<%=userId%>">
              <div>ĐƠN HÀNG</div>
            </a>

            <%}%>
            <div class="header-user">
              <div
                class="header-user-fullname"
                data-bs-toggle="modal"
                data-bs-target="#userInfo"
              >
                <%=userFName%>
              </div>
              <div class="header-logout">
                <a href="/auth/logout"
                  ><i class="fa-solid fa-right-from-bracket"></i
                ></a>
              </div>
            </div>
            <%}%> <%if(!userFName) {%>
            <div class="wrapper-upper-icon">
              <!-- Tìm kiếm -->
                <div class="search size-icon">
                    <i class="bi bi-search GH"></i>
                </div>
                <!--  Đăng nhập -->
                <div class="accoun size-icon">
                    <a href="/auth/login" style="color: #000000;">
                      <i class="bi bi-person GH"></i>
                    </a>
            </div>
            <%}%>
          </div>
        </div>
      </div>
    </header>
    <!-- Modal -->
    <div
      class="modal fade"
      id="userInfo"
      tabindex="-1"
      aria-labelledby="exampleModalLabel"
      aria-hidden="true"
      style="margin-top: 80px; text-align: left"
    >
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">
              Thông tin tài khoản
            </h5>
            <button
              type="button"
              class="btn-close"
              data-bs-dismiss="modal"
              aria-label="Close"
            ></button>
          </div>
          <div class="modal-body" style="color: #000">
            <table>
              <tbody>
                <tr>
                  <td>Tên đăng nhập:</td>
                  <td>
                    <input
                      class="username-input"
                      type="text"
                      disabled
                      value="<%=user?.username%>"
                    />
                  </td>
                </tr>
                <tr>
                  <td>Mật khẩu:</td>
                  <td>
                    <div
                      data-bs-toggle="modal"
                      data-bs-target="#changePassword"
                      class="reset-pw-btn"
                    >
                      Đặt lại mật khẩu
                    </div>
                  </td>
                </tr>
                <tr>
                  <td>Họ tên:</td>
                  <td><input type="text" value="<%=user?.full_name%>" /></td>
                </tr>
                <tr>
                  <td>Số điện thoại:</td>
                  <td><input type="text" value="<%=user?.phone_number%>" /></td>
                </tr>
                <tr>
                  <td>Email:</td>
                  <td><input type="text" value="<%=user?.email%>" /></td>
                </tr>
              </tbody>
            </table>
          </div>
          <div class="modal-footer">
            <button
              type="button"
              class="btn"
              style="background-color: var(--topicColor); color: #fff"
              data-bs-dismiss="modal"
            >
              Lưu
            </button>
          </div>
        </div>
      </div>
    </div>
    <div
      class="modal fade"
      id="changePassword"
      tabindex="-1"
      aria-labelledby="exampleModalLabel"
      aria-hidden="true"
      style="padding-top: 140px; text-align: left"
    >
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">Đặt lại mật khẩu</h5>
            <button
              type="button"
              class="btn-close"
              data-bs-dismiss="modal"
              aria-label="Close"
            ></button>
          </div>
          <div class="modal-body" style="color: #000">
            <table>
              <tbody>
                <tr>
                  <td>Mật khẩu cũ:</td>
                  <td><input type="password" /></td>
                </tr>
                <tr>
                  <td>Mật khẩu mới:</td>
                  <td><input type="password" /></td>
                </tr>
                <tr>
                  <td>Nhập lại mật khẩu mới:</td>
                  <td><input type="password" /></td>
                </tr>
              </tbody>
            </table>
          </div>
          <div class="modal-footer">
            <button
              type="button"
              class="btn"
              style="background-color: var(--topicColor); color: #fff"
              data-bs-dismiss="modal"
            >
              Lưu
            </button>
          </div>
        </div>
      </div>
    </div>

    <script>
      window.onscroll = function () {
        // console.info(document.documentElement.scrollTop);
        var header = document.getElementById('Myheader');
        if (
          document.documentElement.scrollTop > 100 ||
          document.body.scrollTop > 100
        ) {
          header.style.position = 'fixed';
          header.style.left = 0;
          header.style.right = 0;
          header.style.background = 'rgb(69,59,59)';
          header.style.zIndex = 9;
        } else {
          // header.style.position = "relative";
          header.style.backgroundColor = 'rgb(0,0,0,0)';
        }
      };

      document.addEventListener("DOMContentLoaded", function() {
      var box1 = document.querySelector(".box-1");
      var box2 = document.querySelector(".box-2");
      var buttonKhoiPhuc = document.getElementById("Button-khoiphuc");
      var buttonQuayVeDangNhap = document.getElementById("Button-quayvedangnhap");
      var boxContainer = document.querySelector(".login_cart_edit");

      buttonQuayVeDangNhap.addEventListener("click", function() {
        box1.classList.add("active");
        box2.classList.remove("active");
        boxContainer.style.transform = "translateX(0)";
        document.querySelector(".main-box").style.height = (box1.offsetHeight + 50) + "px";
      });

      buttonKhoiPhuc.addEventListener("click", function() {
        box2.classList.add("active");
        box1.classList.remove("active");
        document.querySelector(".main-box").style.height = (box2.offsetHeight - 50 ) + "px";
        boxContainer.style.transform = "translateX(-50%)";
      });
    });

    // phần login
    $(document).ready(function () {
        const password = document.querySelector('[name="password"]');
        const eyeIcon = document.querySelector('.eye-icon');
        eyeIcon.style.display = 'none';

        eyeIcon.addEventListener('click', function (e) {
          if (password.type === 'password') {
            password.type = 'text';
            eyeIcon.innerHTML = '<i class="fa-solid fa-eye-slash"></i>';
          } else {
            password.type = 'password';
            eyeIcon.innerHTML = '<i class="fa-solid fa-eye"></i>';
          }
        });
        password.addEventListener('keyup', function (e) {
          if (e.target.value) {
            eyeIcon.style.display = 'inline';
          } else if (
            eyeIcon.innerHTML == '<i class="fa-solid fa-eye-slash"></i>'
          ) {
            eyeIcon.style.display = 'inline';
          } else {
            eyeIcon.style.display = 'none';
          }
        });
        const loginForm = $('#login-form');
        loginForm.on('submit', async (e) => {
          e.preventDefault();
          $('#login-form button[type="submit"]').html(
            '<div class="form-loader"></div>'
          );
          const formLogin = loginForm.serializeArray();
          const loginData = {};
          formLogin.forEach((field) => {
            loginData[field.name] = field.value;
          });
          try {
            const res = await (
              await fetch('/auth/login', {
                method: 'POST',
                headers: {
                  'Content-Type': 'application/json',
                },
                body: JSON.stringify(loginData),
              })
            ).json();
            if (res.msg) {
              const Toast = Swal.mixin({
                toast: true,
                position: 'top-end',
                showConfirmButton: false,
                timer: 4000,
                timerProgressBar: true,
                didOpen: (toast) => {
                  toast.addEventListener('mouseenter', Swal.stopTimer);
                  toast.addEventListener('mouseleave', Swal.resumeTimer);
                },
              });

              Toast.fire({
                icon: 'error',
                text: res.msg,
              });
              $('#login-form button[type="submit"]').text('Đăng nhập');
            } else {
              const Toast = Swal.mixin({
                toast: true,
                position: 'top-end',
                showConfirmButton: false,
                timer: 2000,
                timerProgressBar: true,
                didOpen: (toast) => {
                  toast.addEventListener('mouseenter', Swal.stopTimer);
                  toast.addEventListener('mouseleave', Swal.resumeTimer);
                },
              });

              Toast.fire({
                icon: 'success',
                text: 'Đăng nhập thành công',
              });
              setTimeout(() => {
                window.location.href = '/';
              }, 2000);
            }
          } catch (err) {
            const Toast = Swal.mixin({
              toast: true,
              position: 'top-end',
              showConfirmButton: false,
              timer: 3000,
              timerProgressBar: true,
              didOpen: (toast) => {
                toast.addEventListener('mouseenter', Swal.stopTimer);
                toast.addEventListener('mouseleave', Swal.resumeTimer);
              },
            });

            Toast.fire({
              icon: 'error',
              text: err,
            });
            $('#login-form button[type="submit"]').text('Đăng nhập');
          }
        });
      });
    </script>
  </body>
</html>
